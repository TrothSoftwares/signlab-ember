From Agents page


<div class="col-md-4">
  <!-- <div class="list-group">
    <div class="list-group-item">
      {{input type="text" placeholder="Search Agent" value=searchTerm class="form-control"}}
    </div>
    {{#each matchingAgents  as |agent|}}
    {{#link-to "agents.agent" agent class="list-group-item "}}
    {{agent.name}}
    <span class="pointer glyphicon"></span>
    {{/link-to}}
    {{/each}}
  </div> -->

  {{#paper-card}}
  {{#paper-card-content }}
  {{paper-input  label="Search Agent" value=searchTerm icon="search"}}
  {{#paper-list}}
    {{#each matchingAgents as |agent|}}
      {{#paper-item class="md-3-line"}}
      {{paper-icon primary=true raised=true icon="person"}}
        <div class="md-list-item-text">
          {{#link-to "agents.agent" agent class="list-group-item "}}
          {{agent.name}}
          {{/link-to}}
        </div>
      {{/paper-item}}
       {{paper-divider}}
    {{/each}}
  {{/paper-list}}
{{/paper-card-content}}
{{/paper-card}}


</div>
<div class="col-md-8">
  <div class="list-group">
    {{outlet}}
  </div>
</div>
