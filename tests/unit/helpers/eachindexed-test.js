import { eachindexed } from '../../../helpers/eachindexed';
import { module, test } from 'qunit';

module('Unit | Helper | eachindexed');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = eachindexed(42);
  assert.ok(result);
});
