import { incrementone } from '../../../helpers/incrementone';
import { module, test } from 'qunit';

module('Unit | Helper | incrementone');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = incrementone(42);
  assert.ok(result);
});
